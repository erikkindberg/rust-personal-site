# Welcome

This is your new static site.

- Write Markdown in the `content/` directory
- Run `cargo run`
- Open generated HTML in `public/`

## Example section

You can use **bold**, *italics*, links, and code.

```rust
fn main() {
    println!("Hello from markdown");
}
```
